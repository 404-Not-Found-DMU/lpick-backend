## 로컬에서 저희가 개발 시 사용될 docker-compose.local.yml입니다.
## 현재는 redis 실행만 담당하고있습니다.
version: "3.8"

services:

  redis:
    image: redis:7.2-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: always

volumes:
  redis-data: